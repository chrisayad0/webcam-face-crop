# webcam-face-crop
python script that allows you to freeeze frame and select a region and it will save the cropped image.


ball-tracker actually tracks anything you select

ball-autotrack added the ability to re-lock on after losing the tracking.
