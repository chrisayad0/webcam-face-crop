# webcam-face-crop
python script that allows you to freeeze frame and select a region and it will save the cropped image.


ball-tracker.py  actually tracks anything you select

ball-autotrack.py has the added the ability to re-lock on after losing the tracking.
